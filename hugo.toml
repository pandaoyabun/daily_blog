baseURL = "https://pandaoyabun.github.io/daily_blog/"
canonifyURLs = true
relativeURLs = true
languageCode = "ja-jp"
title = "おやぶんのアーカイブ"
theme = 'hugo-theme-stack'

removePathAccents = true
summaryLength = 10

# HTMLタグ（アメブロの表やリンク）を許可する
[markup.goldmark.renderer]
    unsafe = true

[params]
    mainSections = ["post"]

    [params.sidebar]
        enabled = true
        [params.sidebar.avatar]
            enabled = true
            # URLを直接書く場合は 'local = false' を明示します
            local = false
            src = "https://github.com/github.png"
        
        emoji = "🚀"
        subtitle = "20年分の記録を奪還中"

    [params.article]
        [params.article.license]
            enabled = false

    [params.footer]
        enabled = true